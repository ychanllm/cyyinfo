# cyyinfo 情侣纪念独立站 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建部署在 Cloudflare 的情侣纪念站（Pages + Hono Worker + D1 + R2），含桌宠、相册、日记、音乐、留言、多账号后台与可选访客口令。

**Architecture:** 镜像 ysoho：`worker/`（Hono + TS，D1 `DB` + R2 `UPLOADS`）提供 `/api` 与 `/uploads`；`web/`（Vue 3 + Vite SPA）部署到 Pages，`web/functions/` 两个 catch-all Pages Function 把 `/api/*`、`/uploads/*` 同源代理到 Worker。桌宠为纯 canvas sprite 播放器，从 `cyy/resources/app/` 拷入 `web/public/pet/` 加适配层。

**Tech Stack:** Hono 4, jose, bcryptjs, wrangler 4, vitest + @cloudflare/vitest-pool-workers；Vue 3 + vue-router + Vite（无 TS、无 UI 框架，与 ysoho 一致）。

## Global Constraints

- 项目根：`D:/vibeProject/kimiProject/cyyinfo/`，已是 git 仓库（main 分支）
- 设计文档：`docs/superpowers/specs/2026-07-30-cyyinfo-design.md`（所有决策以此为准）
- Worker 名 `cyyinfo-api`；`compatibility_flags = ["nodejs_compat"]`
- 错误统一 `{ detail: msg }` + HTTP 状态码
- 敏感配置走 `wrangler secret`：`JWT_SECRET`、`ADMIN_USERNAME`、`ADMIN_PASSWORD`；非敏感走 `[vars]`：`JWT_EXPIRE_HOURS`
- R2 文件名统一 uuid，前缀区分：`photos/`、`music/`、`covers/`
- 限流：登录 5 次/15 分钟/IP、口令验证 5 次/15 分钟/IP、留言 10 条/小时/IP
- 参考实现可直接阅读：`D:/vibeProject/kimiProject/ysoho/worker/src/` 与 `D:/vibeProject/kimiProject/ysoho/web/src/`（结构同构，可对照抄写后改名）
- 桌宠源文件：`D:/vibeProject/kimiProject/cyy/resources/app/renderer.js`、`D:/vibeProject/kimiProject/cyy/resources/app/skins/default/{skin.json,spritesheet.webp}`
- 每个 Task 完成后 commit；commit message 用 `feat:` / `test:` / `chore:` 前缀
- 前端页面文案用中文；代码注释用中文或英文均可，与邻近代码一致

---

### Task 1: Worker 脚手架 + D1 初始迁移

**Files:**
- Create: `worker/package.json`
- Create: `worker/wrangler.toml`
- Create: `worker/tsconfig.json`
- Create: `worker/vitest.config.ts`
- Create: `worker/src/types.ts`
- Create: `worker/src/index.ts`
- Create: `worker/migrations/0001_initial.sql`

**Interfaces:**
- Produces: `Env` 接口（`worker/src/types.ts`）；D1 表结构（见设计 §3）；Hono app 挂在 `/api`、`/uploads` 下

- [ ] **Step 1: 写脚手架文件**

`worker/package.json`：

```json
{
  "name": "cyyinfo-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:create": "wrangler d1 migrations create cyyinfo-db",
    "migrate:apply": "wrangler d1 migrations apply cyyinfo-db --remote",
    "migrate:local": "wrangler d1 migrations apply cyyinfo-db --local",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240925.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}
```

`worker/wrangler.toml`（D1/R2 的 `database_id`、`bucket_name` 在部署任务里由 `wrangler d1 create` / `wrangler r2 bucket create` 生成后回填，本地开发用 `--local` 不需要真实 id，但字段要先占位）：

```toml
name = "cyyinfo-api"
main = "src/index.ts"
compatibility_date = "2024-09-25"
compatibility_flags = ["nodejs_compat"]

[vars]
JWT_EXPIRE_HOURS = "72"

[[d1_databases]]
binding = "DB"
database_name = "cyyinfo-db"
database_id = "REPLACE_AFTER_D1_CREATE"

[[r2_buckets]]
binding = "UPLOADS"
bucket_name = "cyyinfo-uploads"
```

`worker/tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "types": ["@cloudflare/workers-types"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true
  },
  "include": ["src", "test"]
}
```

`worker/vitest.config.ts`：

```ts
import { defineWorkersConfig } from '@cloudflare/vitest-pool-workers/config';

export default defineWorkersConfig({
  test: {
    poolOptions: {
      workers: {
        wrangler: { configPath: './wrangler.toml' },
        miniflare: {
          d1Databases: ['DB'],
          r2Buckets: ['UPLOADS'],
        },
      },
    },
  },
});
```

`worker/src/types.ts`：

```ts
export interface Env {
  DB: D1Database;
  UPLOADS: R2Bucket;
  JWT_SECRET: string;
  JWT_EXPIRE_HOURS: string;
  ADMIN_USERNAME: string;
  ADMIN_PASSWORD: string;
}

export interface AdminPayload {
  sub: number;
  username: string;
  role: 'admin';
}

export interface GuestPayload {
  role: 'guest';
}
```

`worker/src/index.ts`（路由文件在后续任务创建，先写骨架）：

```ts
import { Hono } from 'hono';
import { cors } from 'hono/cors';
import type { Env } from './types';

const app = new Hono<{ Bindings: Env }>();

const ALLOWED_ORIGINS = [
  'http://localhost:5173',
  // 部署后补充 Pages 域名
];

app.use('*', cors({
  origin: (origin) => (ALLOWED_ORIGINS.includes(origin) ? origin : ALLOWED_ORIGINS[0]),
  allowHeaders: ['Content-Type', 'Authorization'],
  allowMethods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  credentials: true,
}));

// 安全响应头
app.use('*', async (c, next) => {
  await next();
  c.header('X-Content-Type-Options', 'nosniff');
  c.header('X-Frame-Options', 'DENY');
  c.header('Referrer-Policy', 'strict-origin-when-cross-origin');
});

app.get('/api/health', (c) => c.json({ ok: true }));

export default app;
```

`worker/migrations/0001_initial.sql`：

```sql
CREATE TABLE admin_users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT NOT NULL UNIQUE,
  password_hash TEXT NOT NULL,
  display_name TEXT NOT NULL DEFAULT '',
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE albums (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  description TEXT NOT NULL DEFAULT '',
  cover_photo_id INTEGER,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE photos (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  album_id INTEGER NOT NULL REFERENCES albums(id) ON DELETE CASCADE,
  filename TEXT NOT NULL,
  caption TEXT NOT NULL DEFAULT '',
  taken_at TEXT,
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE diaries (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  author_id INTEGER NOT NULL REFERENCES admin_users(id),
  title TEXT NOT NULL,
  slug TEXT UNIQUE,
  content_md TEXT NOT NULL DEFAULT '',
  cover_filename TEXT,
  status TEXT NOT NULL DEFAULT 'draft' CHECK(status IN ('draft','published')),
  published_at TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE music_albums (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  cover_filename TEXT,
  year INTEGER,
  sort_order INTEGER NOT NULL DEFAULT 0
);

CREATE TABLE songs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  album_id INTEGER NOT NULL REFERENCES music_albums(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  track_no INTEGER NOT NULL DEFAULT 0,
  filename TEXT NOT NULL,
  duration INTEGER,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  nickname TEXT NOT NULL,
  content TEXT NOT NULL,
  target_type TEXT NOT NULL DEFAULT 'site' CHECK(target_type IN ('diary','photo','site')),
  target_id INTEGER,
  is_approved INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE settings (
  key TEXT PRIMARY KEY,
  value TEXT
);

INSERT INTO music_albums (title, year, sort_order) VALUES
  ('David Tao', 1997, 1),
  ('I''m OK', 1999, 2),
  ('黑色柳丁', 2002, 3);

INSERT INTO settings (key, value) VALUES
  ('site_name', '我们的小站'),
  ('anniversary_date', ''),
  ('site_passcode_hash', '');
```

- [ ] **Step 2: 安装依赖并本地应用迁移**

```bash
cd worker && npm install
npx wrangler d1 migrations apply cyyinfo-db --local
```

Expected: 迁移成功（`🌀 Mapping SQL input into an array of statements` … `✅`）。若提示 database_id 无效，先 `npx wrangler d1 create cyyinfo-db` 把输出 id 回填 wrangler.toml。

- [ ] **Step 3: 冒烟验证**

```bash
cd worker && npx wrangler dev &
curl http://localhost:8787/api/health
```

Expected: `{"ok":true}`。Ctrl+C 停掉 dev server。

- [ ] **Step 4: Commit**

```bash
git add worker/
git commit -m "chore: worker 脚手架 + D1 初始迁移"
```

---

### Task 2: 限流工具 security.ts

**Files:**
- Create: `worker/src/security.ts`
- Test: `worker/test/security.test.ts`

**Interfaces:**
- Produces: `rateLimit(opts: { limit: number; windowSec: number; key: string }): boolean` — 返回 true 表示放行，false 表示超限（单 isolate 内存滑动窗口，与 ysoho `worker/src/security.ts` 同模式）

- [ ] **Step 1: 写失败测试**

`worker/test/security.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { rateLimit } from '../src/security';

describe('rateLimit', () => {
  it('前 N 次放行，第 N+1 次拒绝', () => {
    const key = 'test-key-1';
    for (let i = 0; i < 5; i++) {
      expect(rateLimit({ limit: 5, windowSec: 900, key })).toBe(true);
    }
    expect(rateLimit({ limit: 5, windowSec: 900, key })).toBe(false);
  });

  it('不同 key 互不影响', () => {
    expect(rateLimit({ limit: 1, windowSec: 900, key: 'test-key-2a' })).toBe(true);
    expect(rateLimit({ limit: 1, windowSec: 900, key: 'test-key-2b' })).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd worker && npx vitest run test/security.test.ts
```

Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 security.ts**

`worker/src/security.ts`：

```ts
interface Bucket { timestamps: number[] }
const buckets = new Map<string, Bucket>();

export function rateLimit(opts: { limit: number; windowSec: number; key: string }): boolean {
  const now = Date.now();
  const windowMs = opts.windowSec * 1000;
  let bucket = buckets.get(opts.key);
  if (!bucket) {
    bucket = { timestamps: [] };
    buckets.set(opts.key, bucket);
  }
  bucket.timestamps = bucket.timestamps.filter((t) => now - t < windowMs);
  if (bucket.timestamps.length >= opts.limit) return false;
  bucket.timestamps.push(now);
  return true;
}

export function clientIp(request: Request): string {
  return request.headers.get('CF-Connecting-IP') ?? 'unknown';
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd worker && npx vitest run test/security.test.ts
```

Expected: 2 passed。

- [ ] **Step 5: Commit**

```bash
git add worker/src/security.ts worker/test/security.test.ts
git commit -m "feat: 滑动窗口限流工具"
```

---

### Task 3: 管理员认证（JWT + bcrypt + 首管理员初始化）

**Files:**
- Create: `worker/src/auth.ts`
- Create: `worker/src/routes/admin.ts`（本任务只写 login + auth 中间件，子路由后续任务挂）
- Modify: `worker/src/index.ts`（挂载 `/api/admin`）
- Test: `worker/test/auth.test.ts`

**Interfaces:**
- Produces:
  - `signJwt(env: Env, payload: object, expireHours?: number): Promise<string>`
  - `verifyJwt(env: Env, token: string): Promise<Record<string, unknown> | null>`
  - `adminAuth` Hono 中间件：校验 Bearer JWT 且 `role==='admin'`，把 `adminId`/`username` 放进 `c.set('admin', ...)`
  - `POST /api/admin/login`，body `{username, password}` → `{token, display_name}`；错误 `{detail}`

- [ ] **Step 1: 写失败测试**

`worker/test/auth.test.ts`（vitest-pool-workers 提供 `env`、`SELF`，来自 `cloudflare:test`；测试前在 D1 里手动建表或用 migration helper——推荐在 `worker/test/setup.ts` 用 `env.DB.exec(读 0001_initial.sql 内容)` 建表，vitest.config 加 `setupFiles`）：

```ts
import { env, SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations } from './helpers';

beforeAll(async () => { await applyMigrations(); });

describe('admin auth', () => {
  it('首管理员用 secret 初始化并可登录', async () => {
    const res = await SELF.fetch('http://x/api/admin/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username: env.ADMIN_USERNAME, password: env.ADMIN_PASSWORD }),
    });
    expect(res.status).toBe(200);
    const body = await res.json() as any;
    expect(body.token).toBeTruthy();
  });

  it('密码错误返回 401', async () => {
    const res = await SELF.fetch('http://x/api/admin/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username: env.ADMIN_USERNAME, password: 'wrong' }),
    });
    expect(res.status).toBe(401);
    expect((await res.json() as any).detail).toBeTruthy();
  });

  it('无 token 访问受保护路由返回 401', async () => {
    const res = await SELF.fetch('http://x/api/admin/users');
    expect(res.status).toBe(401);
  });
});
```

`worker/test/helpers.ts`：

```ts
import { env } from 'cloudflare:test';
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';

export async function applyMigrations() {
  const dir = join(__dirname, '..', 'migrations');
  for (const f of readdirSync(dir).sort()) {
    await env.DB.exec(readFileSync(join(dir, f), 'utf-8'));
  }
}

export async function adminToken(): Promise<string> {
  const res = await (await import('cloudflare:test')).SELF.fetch('http://x/api/admin/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username: env.ADMIN_USERNAME, password: env.ADMIN_PASSWORD }),
  });
  return ((await res.json()) as any).token;
}
```

`worker/test/env.d.ts` + vitest 配置补充：在 `vitest.config.ts` 的 `workers` 配置里加 `miniflare: { bindings: { JWT_SECRET: 'test-secret', ADMIN_USERNAME: 'admin', ADMIN_PASSWORD: 'testpass123', JWT_EXPIRE_HOURS: '72' } }`，并在 `test` 下加 `setupFiles: []`（不需要额外 setup 文件，beforeAll 里建表即可）。注意 miniflare d1 每个测试文件独立，多文件各自跑 migration。

- [ ] **Step 2: 跑测试确认失败**

```bash
cd worker && npx vitest run test/auth.test.ts
```

Expected: FAIL（`/api/admin/login` 404）。

- [ ] **Step 3: 实现**

`worker/src/auth.ts`：

```ts
import { SignJWT, jwtVerify } from 'jose';
import type { Context, Next } from 'hono';
import type { Env } from './types';

const encoder = new TextEncoder();

export async function signJwt(env: Env, payload: Record<string, unknown>, expireHours?: number): Promise<string> {
  const hours = expireHours ?? Number(env.JWT_EXPIRE_HOURS || 72);
  return new SignJWT(payload)
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime(`${hours}h`)
    .sign(encoder.encode(env.JWT_SECRET));
}

export async function verifyJwt(env: Env, token: string): Promise<Record<string, unknown> | null> {
  try {
    const { payload } = await jwtVerify(token, encoder.encode(env.JWT_SECRET));
    return payload as Record<string, unknown>;
  } catch {
    return null;
  }
}

export async function adminAuth(c: Context<{ Bindings: Env }>, next: Next) {
  const header = c.req.header('Authorization') ?? '';
  const token = header.replace(/^Bearer\s+/i, '');
  const payload = token ? await verifyJwt(c.env, token) : null;
  if (!payload || payload.role !== 'admin') {
    return c.json({ detail: '未授权' }, 401);
  }
  c.set('admin', { id: payload.sub as number, username: payload.username as string });
  await next();
}
```

`worker/src/routes/admin.ts`：

```ts
import { Hono } from 'hono';
import bcrypt from 'bcryptjs';
import type { Env } from '../types';
import { signJwt, adminAuth } from '../auth';
import { rateLimit, clientIp } from '../security';

const admin = new Hono<{ Bindings: Env }>();

admin.post('/login', async (c) => {
  if (!rateLimit({ limit: 5, windowSec: 900, key: `login:${clientIp(c.req.raw)}` })) {
    return c.json({ detail: '尝试过于频繁，请稍后再试' }, 429);
  }
  const { username, password } = await c.req.json<{ username?: string; password?: string }>();
  if (!username || !password) return c.json({ detail: '请输入账号和密码' }, 400);

  let user = await c.env.DB.prepare('SELECT * FROM admin_users WHERE username = ?')
    .bind(username).first<{ id: number; username: string; password_hash: string; display_name: string }>();

  // 表为空时用 secret 初始化首个管理员
  if (!user) {
    const count = await c.env.DB.prepare('SELECT COUNT(*) AS n FROM admin_users').first<{ n: number }>();
    if (count?.n === 0 && username === c.env.ADMIN_USERNAME) {
      const hash = bcrypt.hashSync(c.env.ADMIN_PASSWORD, 10);
      await c.env.DB.prepare('INSERT INTO admin_users (username, password_hash, display_name) VALUES (?, ?, ?)')
        .bind(username, hash, username).run();
      user = await c.env.DB.prepare('SELECT * FROM admin_users WHERE username = ?')
        .bind(username).first<typeof user & object>() as typeof user;
    }
  }
  if (!user || !bcrypt.compareSync(password, user.password_hash)) {
    return c.json({ detail: '账号或密码错误' }, 401);
  }
  const token = await signJwt(c.env, { sub: user.id, username: user.username, role: 'admin' });
  return c.json({ token, display_name: user.display_name });
});

// 受保护子路由在此挂载（后续任务）：admin.use('/users/*', adminAuth) 等
admin.use('/users', adminAuth);
admin.use('/users/*', adminAuth);

export default admin;
```

`worker/src/index.ts` 追加（在 health 路由后）：

```ts
import adminRoutes from './routes/admin';
app.route('/api/admin', adminRoutes);
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd worker && npx vitest run test/auth.test.ts test/security.test.ts
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/src/auth.ts worker/src/routes/admin.ts worker/src/index.ts worker/test/ worker/vitest.config.ts
git commit -m "feat: 管理员登录（JWT + bcrypt + 首管理员初始化 + 限流）"
```

---

### Task 4: 访客口令 + 公开内容守卫 + 站点状态

**Files:**
- Create: `worker/src/guard.ts`
- Create: `worker/src/routes/public.ts`
- Modify: `worker/src/index.ts`（挂载 `/api`）
- Test: `worker/test/passcode.test.ts`

**Interfaces:**
- Consumes: `signJwt`/`verifyJwt`（Task 3）、`rateLimit`（Task 2）
- Produces:
  - `contentGuard` 中间件：若 `settings.site_passcode_hash` 为空 → 放行；否则要求有效 JWT（admin 或 guest 角色均可），否则 401 `{detail:'需要访客口令'}`
  - `POST /api/passcode/verify` body `{passcode}` → `{token}`（guest JWT，7 天）；限流 5 次/15 分钟/IP
  - `GET /api/site/status`（始终公开）→ `{site_name, passcode_enabled, anniversary_date}`
  - `getSetting(db, key)` / `setSetting(db, key, value)` helper

- [ ] **Step 1: 写失败测试**

`worker/test/passcode.test.ts`：

```ts
import { env, SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations } from './helpers';
import bcrypt from 'bcryptjs';

beforeAll(async () => { await applyMigrations(); });

describe('访客口令', () => {
  it('口令未设置时公开接口直接放行', async () => {
    const res = await SELF.fetch('http://x/api/albums');
    expect(res.status).toBe(200);
  });

  it('site/status 始终公开', async () => {
    const res = await SELF.fetch('http://x/api/site/status');
    expect(res.status).toBe(200);
    const body = await res.json() as any;
    expect(body.passcode_enabled).toBe(false);
  });

  it('设置口令后公开接口返回 401，验证口令后可访问', async () => {
    await env.DB.prepare('UPDATE settings SET value = ? WHERE key = ?')
      .bind(bcrypt.hashSync('iloveu', 10), 'site_passcode_hash').run();

    const blocked = await SELF.fetch('http://x/api/albums');
    expect(blocked.status).toBe(401);

    const bad = await SELF.fetch('http://x/api/passcode/verify', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ passcode: 'wrong' }),
    });
    expect(bad.status).toBe(401);

    const ok = await SELF.fetch('http://x/api/passcode/verify', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ passcode: 'iloveu' }),
    });
    expect(ok.status).toBe(200);
    const { token } = await ok.json() as any;

    const authed = await SELF.fetch('http://x/api/albums', {
      headers: { Authorization: `Bearer ${token}` },
    });
    expect(authed.status).toBe(200);

    // 还原，避免影响其他用例
    await env.DB.prepare('UPDATE settings SET value = ? WHERE key = ?')
      .bind('', 'site_passcode_hash').run();
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd worker && npx vitest run test/passcode.test.ts
```

Expected: FAIL（路由 404）。

- [ ] **Step 3: 实现**

`worker/src/guard.ts`：

```ts
import type { Context, Next } from 'hono';
import type { Env } from './types';
import { verifyJwt } from './auth';

export async function getSetting(db: D1Database, key: string): Promise<string> {
  const row = await db.prepare('SELECT value FROM settings WHERE key = ?').bind(key).first<{ value: string }>();
  return row?.value ?? '';
}

export async function setSetting(db: D1Database, key: string, value: string): Promise<void> {
  await db.prepare('INSERT INTO settings (key, value) VALUES (?, ?) ON CONFLICT(key) DO UPDATE SET value = excluded.value')
    .bind(key, value).run();
}

// 公开内容守卫：口令为空放行，否则要求 admin/guest JWT
export async function contentGuard(c: Context<{ Bindings: Env }>, next: Next) {
  const hash = await getSetting(c.env.DB, 'site_passcode_hash');
  if (!hash) return next();
  const header = c.req.header('Authorization') ?? '';
  const token = header.replace(/^Bearer\s+/i, '');
  const payload = token ? await verifyJwt(c.env, token) : null;
  if (!payload || (payload.role !== 'guest' && payload.role !== 'admin')) {
    return c.json({ detail: '需要访客口令' }, 401);
  }
  await next();
}
```

`worker/src/routes/public.ts`：

```ts
import { Hono } from 'hono';
import bcrypt from 'bcryptjs';
import type { Env } from '../types';
import { signJwt } from '../auth';
import { rateLimit, clientIp } from '../security';
import { contentGuard, getSetting } from '../guard';

const pub = new Hono<{ Bindings: Env }>();

pub.get('/site/status', async (c) => {
  const hash = await getSetting(c.env.DB, 'site_passcode_hash');
  return c.json({
    site_name: await getSetting(c.env.DB, 'site_name'),
    anniversary_date: await getSetting(c.env.DB, 'anniversary_date'),
    passcode_enabled: Boolean(hash),
  });
});

pub.post('/passcode/verify', async (c) => {
  if (!rateLimit({ limit: 5, windowSec: 900, key: `passcode:${clientIp(c.req.raw)}` })) {
    return c.json({ detail: '尝试过于频繁，请稍后再试' }, 429);
  }
  const hash = await getSetting(c.env.DB, 'site_passcode_hash');
  if (!hash) return c.json({ detail: '站点未启用访客口令' }, 400);
  const { passcode } = await c.req.json<{ passcode?: string }>();
  if (!passcode || !bcrypt.compareSync(passcode, hash)) {
    return c.json({ detail: '口令错误' }, 401);
  }
  const token = await signJwt(c.env, { role: 'guest' }, 24 * 7);
  return c.json({ token });
});

// 内容接口挂在 contentGuard 之后（后续任务在此追加路由）
const content = new Hono<{ Bindings: Env }>();
content.use('*', contentGuard);
content.get('/albums', async (c) => {
  const { results } = await c.env.DB.prepare(
    'SELECT a.*, p.filename AS cover_filename FROM albums a LEFT JOIN photos p ON p.id = a.cover_photo_id ORDER BY a.sort_order, a.id'
  ).all();
  return c.json(results);
});

pub.route('/', content);
export default pub;
```

`worker/src/index.ts` 追加：`import publicRoutes from './routes/public'; app.route('/api', publicRoutes);`（注意 `app.route('/api/admin', adminRoutes)` 要在 `/api` 之前挂载，Hono 按注册顺序匹配）。

- [ ] **Step 4: 跑测试确认通过**

```bash
cd worker && npx vitest run
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/src/guard.ts worker/src/routes/public.ts worker/src/index.ts worker/test/passcode.test.ts
git commit -m "feat: 访客口令 + 公开内容守卫 + 站点状态接口"
```

---

### Task 5: 相册 + 照片（管理端 CRUD、R2 上传、/uploads 读取）

**Files:**
- Create: `worker/src/routes/storage.ts`
- Modify: `worker/src/routes/admin.ts`（挂 albums/photos 子路由）
- Modify: `worker/src/routes/public.ts`（`GET /api/albums/:id`）
- Modify: `worker/src/index.ts`（挂 `/uploads`）
- Test: `worker/test/photos.test.ts`

**Interfaces:**
- Consumes: `adminAuth`（Task 3）、`contentGuard`（Task 4 已挂在 content 组上）
- Produces:
  - `GET /uploads/:path` — 从 R2 读文件，30 天缓存头
  - 管理端：`GET/POST/PUT/DELETE /api/admin/albums(/:id)`；`POST /api/admin/photos`（multipart：`file`、`album_id`、`caption`）；`PUT/DELETE /api/admin/photos/:id`；`POST /api/admin/albums/:id/cover` body `{photo_id}`
  - 公开端（Task 4 已有 `GET /api/albums`）：`GET /api/albums/:id` → 相册 + `photos[]`
  - 上传 helper `saveUpload(env, file, prefix): Promise<string>`（uuid 文件名，校验 MIME/大小，返回 R2 key）

- [ ] **Step 1: 写失败测试**

`worker/test/photos.test.ts`：

```ts
import { SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations, adminToken } from './helpers';

let token: string;
beforeAll(async () => { await applyMigrations(); token = await adminToken(); });

const auth = () => ({ Authorization: `Bearer ${token}` });

describe('相册与照片', () => {
  it('创建相册 → 上传照片 → 公开读取 → /uploads 读图', async () => {
    // 无 token 创建应 401
    expect((await SELF.fetch('http://x/api/admin/albums', { method: 'POST' })).status).toBe(401);

    const create = await SELF.fetch('http://x/api/admin/albums', {
      method: 'POST', headers: { ...auth(), 'Content-Type': 'application/json' },
      body: JSON.stringify({ title: '旅行', description: '2026' }),
    });
    expect(create.status).toBe(200);
    const album = await create.json() as any;
    expect(album.id).toBeTruthy();

    // 上传照片（构造一个最小合法 JPEG 字节）
    const jpeg = new Uint8Array([0xff, 0xd8, 0xff, 0xe0, 0x00, 0x10, 0x4a, 0x46, 0x49, 0x46, 0x00, 0x01, 0xff, 0xd9]);
    const form = new FormData();
    form.append('file', new File([jpeg], 'a.jpg', { type: 'image/jpeg' }));
    form.append('album_id', String(album.id));
    form.append('caption', '海边');
    const up = await SELF.fetch('http://x/api/admin/photos', { method: 'POST', headers: auth(), body: form });
    expect(up.status).toBe(200);
    const photo = await up.json() as any;
    expect(photo.filename).toMatch(/^photos\//);

    // 公开接口读到
    const detail = await SELF.fetch(`http://x/api/albums/${album.id}`);
    expect(detail.status).toBe(200);
    const d = await detail.json() as any;
    expect(d.photos).toHaveLength(1);
    expect(d.photos[0].caption).toBe('海边');

    // /uploads 可读
    const img = await SELF.fetch(`http://x/uploads/${photo.filename}`);
    expect(img.status).toBe(200);
    expect(img.headers.get('Cache-Control')).toContain('max-age');
  });

  it('拒绝非图片类型', async () => {
    const form = new FormData();
    form.append('file', new File([new Uint8Array([1, 2, 3])], 'x.exe', { type: 'application/octet-stream' }));
    form.append('album_id', '1');
    const res = await SELF.fetch('http://x/api/admin/photos', { method: 'POST', headers: auth(), body: form });
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd worker && npx vitest run test/photos.test.ts
```

Expected: FAIL。

- [ ] **Step 3: 实现**

`worker/src/routes/storage.ts`：

```ts
import { Hono } from 'hono';
import type { Env } from '../types';

const storage = new Hono<{ Bindings: Env }>();

storage.get('/:path{.+}', async (c) => {
  const key = c.req.param('path');
  const obj = await c.env.UPLOADS.get(key);
  if (!obj) return c.json({ detail: '文件不存在' }, 404);
  const headers = new Headers();
  headers.set('Content-Type', obj.httpMetadata?.contentType ?? 'application/octet-stream');
  headers.set('Cache-Control', 'public, max-age=2592000');
  headers.set('ETag', obj.httpEtag);
  return new Response(obj.body, { headers });
});

export default storage;
```

`worker/src/upload.ts`：

```ts
import type { Env } from './types';

const IMAGE_TYPES: Record<string, string> = {
  'image/jpeg': '.jpg', 'image/png': '.png', 'image/webp': '.webp', 'image/gif': '.gif',
};
const AUDIO_TYPES: Record<string, string> = {
  'audio/mpeg': '.mp3', 'audio/mp4': '.m4a', 'audio/x-m4a': '.m4a',
};
const MAX_IMAGE = 10 * 1024 * 1024;   // 10MB
const MAX_AUDIO = 30 * 1024 * 1024;   // 30MB

export async function saveUpload(
  env: Env, file: File, kind: 'image' | 'audio', prefix: string,
): Promise<{ key?: string; error?: string }> {
  const table = kind === 'image' ? IMAGE_TYPES : AUDIO_TYPES;
  const ext = table[file.type];
  if (!ext) return { error: '不支持的文件类型' };
  if (file.size > (kind === 'image' ? MAX_IMAGE : MAX_AUDIO)) return { error: '文件过大' };
  const key = `${prefix}/${crypto.randomUUID()}${ext}`;
  await env.UPLOADS.put(key, await file.arrayBuffer(), { httpMetadata: { contentType: file.type } });
  return { key };
}
```

`worker/src/routes/admin.ts` 在 `admin.use('/users', adminAuth)` 之前插入（保证 login 不需鉴权，其余都要）：

```ts
admin.use('/albums', adminAuth);
admin.use('/albums/*', adminAuth);
admin.use('/photos', adminAuth);
admin.use('/photos/*', adminAuth);

// ---- 相册 CRUD ----
admin.get('/albums', async (c) => {
  const { results } = await c.env.DB.prepare('SELECT * FROM albums ORDER BY sort_order, id').all();
  return c.json(results);
});

admin.post('/albums', async (c) => {
  const { title, description = '', sort_order = 0 } = await c.req.json();
  if (!title) return c.json({ detail: '标题必填' }, 400);
  const r = await c.env.DB.prepare('INSERT INTO albums (title, description, sort_order) VALUES (?, ?, ?)')
    .bind(title, description, sort_order).run();
  return c.json({ id: r.meta.last_row_id, title, description, sort_order });
});

admin.put('/albums/:id', async (c) => {
  const { title, description, sort_order } = await c.req.json();
  await c.env.DB.prepare('UPDATE albums SET title = COALESCE(?, title), description = COALESCE(?, description), sort_order = COALESCE(?, sort_order) WHERE id = ?')
    .bind(title ?? null, description ?? null, sort_order ?? null, c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.delete('/albums/:id', async (c) => {
  // 先删 R2 里的照片文件
  const { results } = await c.env.DB.prepare('SELECT filename FROM photos WHERE album_id = ?')
    .bind(c.req.param('id')).all<{ filename: string }>();
  for (const p of results) await c.env.UPLOADS.delete(p.filename);
  await c.env.DB.prepare('DELETE FROM albums WHERE id = ?').bind(c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.post('/albums/:id/cover', async (c) => {
  const { photo_id } = await c.req.json();
  await c.env.DB.prepare('UPDATE albums SET cover_photo_id = ? WHERE id = ?')
    .bind(photo_id, c.req.param('id')).run();
  return c.json({ ok: true });
});

// ---- 照片 ----
admin.post('/photos', async (c) => {
  const body = await c.req.parseBody();
  const file = body.file;
  const albumId = Number(body.album_id);
  const caption = String(body.caption ?? '');
  if (!(file instanceof File) || !albumId) return c.json({ detail: '缺少文件或相册' }, 400);
  const { key, error } = await saveUpload(c.env, file, 'image', 'photos');
  if (error) return c.json({ detail: error }, 400);
  const r = await c.env.DB.prepare('INSERT INTO photos (album_id, filename, caption) VALUES (?, ?, ?)')
    .bind(albumId, key!, caption).run();
  return c.json({ id: r.meta.last_row_id, filename: key, album_id: albumId, caption });
});

admin.put('/photos/:id', async (c) => {
  const { caption, sort_order, taken_at } = await c.req.json();
  await c.env.DB.prepare('UPDATE photos SET caption = COALESCE(?, caption), sort_order = COALESCE(?, sort_order), taken_at = COALESCE(?, taken_at) WHERE id = ?')
    .bind(caption ?? null, sort_order ?? null, taken_at ?? null, c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.delete('/photos/:id', async (c) => {
  const photo = await c.env.DB.prepare('SELECT filename FROM photos WHERE id = ?')
    .bind(c.req.param('id')).first<{ filename: string }>();
  if (photo) await c.env.UPLOADS.delete(photo.filename);
  await c.env.DB.prepare('DELETE FROM photos WHERE id = ?').bind(c.req.param('id')).run();
  return c.json({ ok: true });
});
```

文件顶部补 `import { saveUpload } from '../upload';`。

`worker/src/routes/public.ts` 在 content 组追加：

```ts
content.get('/albums/:id', async (c) => {
  const album = await c.env.DB.prepare(
    'SELECT a.*, p.filename AS cover_filename FROM albums a LEFT JOIN photos p ON p.id = a.cover_photo_id WHERE a.id = ?'
  ).bind(c.req.param('id')).first();
  if (!album) return c.json({ detail: '相册不存在' }, 404);
  const { results: photos } = await c.env.DB.prepare(
    'SELECT id, filename, caption, taken_at, sort_order FROM photos WHERE album_id = ? ORDER BY sort_order, id'
  ).bind(c.req.param('id')).all();
  return c.json({ ...album, photos });
});
```

`worker/src/index.ts` 追加：`import storageRoutes from './routes/storage'; app.route('/uploads', storageRoutes);`

- [ ] **Step 4: 跑测试确认通过**

```bash
cd worker && npx vitest run
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/src/routes/ worker/src/upload.ts worker/src/index.ts worker/test/photos.test.ts
git commit -m "feat: 相册/照片 CRUD + R2 上传 + /uploads 读取"
```

---

### Task 6: 日记（管理端 CRUD + 公开阅读）

**Files:**
- Modify: `worker/src/routes/admin.ts`（挂 diaries 子路由 + 封面上传）
- Modify: `worker/src/routes/public.ts`（diaries 列表/详情）
- Test: `worker/test/diaries.test.ts`

**Interfaces:**
- Produces:
  - 管理端：`GET /api/admin/diaries`（含草稿）、`POST /api/admin/diaries`、`PUT/DELETE /api/admin/diaries/:id`、`POST /api/admin/diaries/:id/cover`（multipart file → `covers/` 前缀）
  - 公开端：`GET /api/diaries?page=1`（每页 10 条，仅 published，返回 `{items, total}`，不含 content_md 全文，只返回 `excerpt` = 前 200 字符）；`GET /api/diaries/:slugOrId`（slug 优先，回退数字 id；仅 published）

- [ ] **Step 1: 写失败测试**

```ts
import { SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations, adminToken } from './helpers';

let token: string;
beforeAll(async () => { await applyMigrations(); token = await adminToken(); });
const auth = () => ({ Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' });

describe('日记', () => {
  it('草稿不出现在公开列表，发布后可按 slug 阅读', async () => {
    const create = await SELF.fetch('http://x/api/admin/diaries', {
      method: 'POST', headers: auth(),
      body: JSON.stringify({ title: '第一篇', content_md: '# 你好\n\n正文内容', slug: 'first' }),
    });
    expect(create.status).toBe(200);
    const diary = await create.json() as any;

    let list = await (await SELF.fetch('http://x/api/diaries')).json() as any;
    expect(list.items).toHaveLength(0); // 草稿

    const pub = await SELF.fetch(`http://x/api/admin/diaries/${diary.id}`, {
      method: 'PUT', headers: auth(), body: JSON.stringify({ status: 'published' }),
    });
    expect(pub.status).toBe(200);

    list = await (await SELF.fetch('http://x/api/diaries')).json() as any;
    expect(list.items).toHaveLength(1);
    expect(list.items[0].excerpt).toBeTruthy();
    expect(list.items[0].content_md).toBeUndefined();

    const read = await SELF.fetch('http://x/api/diaries/first');
    expect(read.status).toBe(200);
    expect((await read.json() as any).content_md).toContain('正文内容');

    const byId = await SELF.fetch(`http://x/api/diaries/${diary.id}`);
    expect(byId.status).toBe(200);
  });
});
```

- [ ] **Step 2: 跑测试确认失败** — `cd worker && npx vitest run test/diaries.test.ts` → FAIL。

- [ ] **Step 3: 实现**

`worker/src/routes/admin.ts` 追加（middleware 声明处加 `admin.use('/diaries', adminAuth); admin.use('/diaries/*', adminAuth);`）：

```ts
admin.get('/diaries', async (c) => {
  const { results } = await c.env.DB.prepare(
    'SELECT id, title, slug, status, cover_filename, published_at, created_at, updated_at FROM diaries ORDER BY id DESC'
  ).all();
  return c.json(results);
});

admin.post('/diaries', async (c) => {
  const adminUser = c.get('admin') as { id: number };
  const { title, content_md = '', slug = null, status = 'draft' } = await c.req.json();
  if (!title) return c.json({ detail: '标题必填' }, 400);
  if (slug) {
    const dup = await c.env.DB.prepare('SELECT id FROM diaries WHERE slug = ?').bind(slug).first();
    if (dup) return c.json({ detail: 'slug 已被占用' }, 400);
  }
  const publishedAt = status === 'published' ? new Date().toISOString() : null;
  const r = await c.env.DB.prepare(
    'INSERT INTO diaries (author_id, title, slug, content_md, status, published_at) VALUES (?, ?, ?, ?, ?, ?)'
  ).bind(adminUser.id, title, slug, content_md, status, publishedAt).run();
  return c.json({ id: r.meta.last_row_id });
});

admin.put('/diaries/:id', async (c) => {
  const { title, content_md, slug, status } = await c.req.json();
  const old = await c.env.DB.prepare('SELECT * FROM diaries WHERE id = ?').bind(c.req.param('id'))
    .first<{ status: string; published_at: string | null }>();
  if (!old) return c.json({ detail: '日记不存在' }, 404);
  if (slug) {
    const dup = await c.env.DB.prepare('SELECT id FROM diaries WHERE slug = ? AND id != ?')
      .bind(slug, c.req.param('id')).first();
    if (dup) return c.json({ detail: 'slug 已被占用' }, 400);
  }
  // 首次发布时记录 published_at
  const publishedAt = status === 'published' && old.status !== 'published'
    ? new Date().toISOString() : old.published_at;
  await c.env.DB.prepare(
    `UPDATE diaries SET title = COALESCE(?, title), content_md = COALESCE(?, content_md),
     slug = COALESCE(?, slug), status = COALESCE(?, status), published_at = ?,
     updated_at = datetime('now') WHERE id = ?`
  ).bind(title ?? null, content_md ?? null, slug ?? null, status ?? null, publishedAt, c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.delete('/diaries/:id', async (c) => {
  const d = await c.env.DB.prepare('SELECT cover_filename FROM diaries WHERE id = ?')
    .bind(c.req.param('id')).first<{ cover_filename: string | null }>();
  if (d?.cover_filename) await c.env.UPLOADS.delete(d.cover_filename);
  await c.env.DB.prepare('DELETE FROM diaries WHERE id = ?').bind(c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.post('/diaries/:id/cover', async (c) => {
  const body = await c.req.parseBody();
  const file = body.file;
  if (!(file instanceof File)) return c.json({ detail: '缺少文件' }, 400);
  const { key, error } = await saveUpload(c.env, file, 'image', 'covers');
  if (error) return c.json({ detail: error }, 400);
  await c.env.DB.prepare('UPDATE diaries SET cover_filename = ? WHERE id = ?')
    .bind(key!, c.req.param('id')).run();
  return c.json({ cover_filename: key });
});
```

`worker/src/routes/public.ts` content 组追加：

```ts
content.get('/diaries', async (c) => {
  const page = Math.max(1, Number(c.req.query('page') ?? 1));
  const size = 10;
  const total = await c.env.DB.prepare("SELECT COUNT(*) AS n FROM diaries WHERE status = 'published'")
    .first<{ n: number }>();
  const { results } = await c.env.DB.prepare(
    `SELECT d.id, d.title, d.slug, d.cover_filename, d.published_at, u.display_name AS author,
            substr(d.content_md, 1, 200) AS excerpt
     FROM diaries d JOIN admin_users u ON u.id = d.author_id
     WHERE d.status = 'published' ORDER BY d.published_at DESC LIMIT ? OFFSET ?`
  ).bind(size, (page - 1) * size).all();
  return c.json({ items: results, total: total?.n ?? 0 });
});

content.get('/diaries/:slugOrId', async (c) => {
  const key = c.req.param('slugOrId');
  const isId = /^\d+$/.test(key);
  const d = await c.env.DB.prepare(
    `SELECT d.id, d.title, d.slug, d.content_md, d.cover_filename, d.published_at, u.display_name AS author
     FROM diaries d JOIN admin_users u ON u.id = d.author_id
     WHERE d.status = 'published' AND ${isId ? 'd.id = ?' : 'd.slug = ?'}`
  ).bind(key).first();
  if (!d) return c.json({ detail: '文章不存在' }, 404);
  return c.json(d);
});
```

- [ ] **Step 4: 跑测试确认通过** — `cd worker && npx vitest run` → 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/src/routes/ worker/test/diaries.test.ts
git commit -m "feat: 日记 CRUD + 公开阅读（草稿/发布、slug、摘要分页）"
```

---

### Task 7: 音乐（专辑/歌曲管理 + 公开列表）

**Files:**
- Modify: `worker/src/routes/admin.ts`（music 子路由）
- Modify: `worker/src/routes/public.ts`（music 公开路由）
- Test: `worker/test/music.test.ts`

**Interfaces:**
- Produces:
  - 管理端：`GET/POST/PUT/DELETE /api/admin/music/albums(/:id)`；`POST /api/admin/music/songs`（multipart：`file`（mp3/m4a）、`album_id`、`title`、`track_no`）；`PUT/DELETE /api/admin/music/songs/:id`；专辑封面 `POST /api/admin/music/albums/:id/cover`（multipart → `covers/`）
  - 公开端：`GET /api/music/albums`（含 `song_count`）；`GET /api/music/albums/:id`（专辑 + `songs[]`，按 track_no 排序）

- [ ] **Step 1: 写失败测试**

```ts
import { SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations, adminToken } from './helpers';

let token: string;
beforeAll(async () => { await applyMigrations(); token = await adminToken(); });
const auth = () => ({ Authorization: `Bearer ${token}` });

describe('音乐', () => {
  it('预置三张专辑，可上传歌曲并按曲目号排序读取', async () => {
    const albums = await (await SELF.fetch('http://x/api/music/albums')).json() as any[];
    expect(albums).toHaveLength(3);
    expect(albums.map((a) => a.title)).toEqual(['David Tao', "I'm OK", '黑色柳丁']);

    const mp3 = new Uint8Array([0xff, 0xfb, 0x90, 0x00, 0x00, 0x00, 0x00, 0x00]);
    for (const [no, title] of [[2, '黑色柳丁'], [1, 'Dear God']] as const) {
      const form = new FormData();
      form.append('file', new File([mp3], `${title}.mp3`, { type: 'audio/mpeg' }));
      form.append('album_id', String(albums[2].id));
      form.append('title', title);
      form.append('track_no', String(no));
      const res = await SELF.fetch('http://x/api/admin/music/songs', { method: 'POST', headers: auth(), body: form });
      expect(res.status).toBe(200);
    }

    const detail = await (await SELF.fetch(`http://x/api/music/albums/${albums[2].id}`)).json() as any;
    expect(detail.songs.map((s: any) => s.title)).toEqual(['Dear God', '黑色柳丁']);
  });
});
```

- [ ] **Step 2: 跑测试确认失败** — `cd worker && npx vitest run test/music.test.ts` → FAIL。

- [ ] **Step 3: 实现**

`worker/src/routes/admin.ts` 追加（middleware 处加 `admin.use('/music', adminAuth); admin.use('/music/*', adminAuth);`）：

```ts
// ---- 音乐专辑 ----
admin.get('/music/albums', async (c) => {
  const { results } = await c.env.DB.prepare('SELECT * FROM music_albums ORDER BY sort_order, id').all();
  return c.json(results);
});

admin.put('/music/albums/:id', async (c) => {
  const { title, year, sort_order } = await c.req.json();
  await c.env.DB.prepare('UPDATE music_albums SET title = COALESCE(?, title), year = COALESCE(?, year), sort_order = COALESCE(?, sort_order) WHERE id = ?')
    .bind(title ?? null, year ?? null, sort_order ?? null, c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.post('/music/albums/:id/cover', async (c) => {
  const body = await c.req.parseBody();
  const file = body.file;
  if (!(file instanceof File)) return c.json({ detail: '缺少文件' }, 400);
  const { key, error } = await saveUpload(c.env, file, 'image', 'covers');
  if (error) return c.json({ detail: error }, 400);
  await c.env.DB.prepare('UPDATE music_albums SET cover_filename = ? WHERE id = ?')
    .bind(key!, c.req.param('id')).run();
  return c.json({ cover_filename: key });
});

// ---- 歌曲 ----
admin.post('/music/songs', async (c) => {
  const body = await c.req.parseBody();
  const file = body.file;
  const albumId = Number(body.album_id);
  const title = String(body.title ?? '');
  const trackNo = Number(body.track_no ?? 0);
  if (!(file instanceof File) || !albumId || !title) return c.json({ detail: '缺少文件/专辑/歌名' }, 400);
  const { key, error } = await saveUpload(c.env, file, 'audio', 'music');
  if (error) return c.json({ detail: error }, 400);
  const r = await c.env.DB.prepare('INSERT INTO songs (album_id, title, track_no, filename) VALUES (?, ?, ?, ?)')
    .bind(albumId, title, trackNo, key!).run();
  return c.json({ id: r.meta.last_row_id, filename: key });
});

admin.put('/music/songs/:id', async (c) => {
  const { title, track_no } = await c.req.json();
  await c.env.DB.prepare('UPDATE songs SET title = COALESCE(?, title), track_no = COALESCE(?, track_no) WHERE id = ?')
    .bind(title ?? null, track_no ?? null, c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.delete('/music/songs/:id', async (c) => {
  const s = await c.env.DB.prepare('SELECT filename FROM songs WHERE id = ?')
    .bind(c.req.param('id')).first<{ filename: string }>();
  if (s) await c.env.UPLOADS.delete(s.filename);
  await c.env.DB.prepare('DELETE FROM songs WHERE id = ?').bind(c.req.param('id')).run();
  return c.json({ ok: true });
});
```

`worker/src/routes/public.ts` content 组追加：

```ts
content.get('/music/albums', async (c) => {
  const { results } = await c.env.DB.prepare(
    `SELECT m.*, COUNT(s.id) AS song_count FROM music_albums m
     LEFT JOIN songs s ON s.album_id = m.id GROUP BY m.id ORDER BY m.sort_order, m.id`
  ).all();
  return c.json(results);
});

content.get('/music/albums/:id', async (c) => {
  const album = await c.env.DB.prepare('SELECT * FROM music_albums WHERE id = ?')
    .bind(c.req.param('id')).first();
  if (!album) return c.json({ detail: '专辑不存在' }, 404);
  const { results: songs } = await c.env.DB.prepare(
    'SELECT id, title, track_no, filename, duration FROM songs WHERE album_id = ? ORDER BY track_no, id'
  ).bind(c.req.param('id')).all();
  return c.json({ ...album, songs });
});
```

- [ ] **Step 4: 跑测试确认通过** — `cd worker && npx vitest run` → 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/src/routes/ worker/test/music.test.ts
git commit -m "feat: 音乐专辑/歌曲管理 + 公开列表（预置陶喆三专）"
```

---

### Task 8: 留言（访客提交 + 审核）

**Files:**
- Modify: `worker/src/routes/public.ts`（messages 提交/读取）
- Modify: `worker/src/routes/admin.ts`（审核/删除）
- Test: `worker/test/messages.test.ts`

**Interfaces:**
- Produces:
  - 公开端（contentGuard 内）：`GET /api/messages?target_type=&target_id=`（仅 `is_approved=1`，倒序）；`POST /api/messages` body `{nickname, content, target_type, target_id?}` → 202 `{detail:'留言已提交，待审核'}`；nickname ≤ 20 字符、content ≤ 500 字符；限流 10 条/小时/IP
  - 管理端：`GET /api/admin/messages?pending=1`、`POST /api/admin/messages/:id/approve`、`DELETE /api/admin/messages/:id`

- [ ] **Step 1: 写失败测试**

```ts
import { SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations, adminToken } from './helpers';

let token: string;
beforeAll(async () => { await applyMigrations(); token = await adminToken(); });

describe('留言', () => {
  it('提交后待审核不可见，审核后可见', async () => {
    const post = await SELF.fetch('http://x/api/messages', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ nickname: '小明', content: '祝幸福！', target_type: 'site' }),
    });
    expect(post.status).toBe(202);

    let list = await (await SELF.fetch('http://x/api/messages?target_type=site')).json() as any[];
    expect(list).toHaveLength(0);

    const pending = await (await SELF.fetch('http://x/api/admin/messages?pending=1', {
      headers: { Authorization: `Bearer ${token}` },
    })).json() as any[];
    expect(pending).toHaveLength(1);

    const approve = await SELF.fetch(`http://x/api/admin/messages/${pending[0].id}/approve`, {
      method: 'POST', headers: { Authorization: `Bearer ${token}` },
    });
    expect(approve.status).toBe(200);

    list = await (await SELF.fetch('http://x/api/messages?target_type=site')).json() as any[];
    expect(list).toHaveLength(1);
    expect(list[0].content).toBe('祝幸福！');
  });

  it('超长内容被拒绝', async () => {
    const res = await SELF.fetch('http://x/api/messages', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ nickname: 'x', content: 'a'.repeat(501), target_type: 'site' }),
    });
    expect(res.status).toBe(400);
  });
});
```

- [ ] **Step 2: 跑测试确认失败** — `cd worker && npx vitest run test/messages.test.ts` → FAIL。

- [ ] **Step 3: 实现**

`worker/src/routes/public.ts` content 组追加：

```ts
content.get('/messages', async (c) => {
  const type = c.req.query('target_type') ?? 'site';
  const targetId = c.req.query('target_id');
  const sql = targetId
    ? 'SELECT id, nickname, content, created_at FROM messages WHERE is_approved = 1 AND target_type = ? AND target_id = ? ORDER BY id DESC LIMIT 100'
    : 'SELECT id, nickname, content, created_at FROM messages WHERE is_approved = 1 AND target_type = ? ORDER BY id DESC LIMIT 100';
  const stmt = targetId
    ? c.env.DB.prepare(sql).bind(type, Number(targetId))
    : c.env.DB.prepare(sql).bind(type);
  return c.json((await stmt.all()).results);
});

content.post('/messages', async (c) => {
  if (!rateLimit({ limit: 10, windowSec: 3600, key: `msg:${clientIp(c.req.raw)}` })) {
    return c.json({ detail: '留言过于频繁，请稍后再试' }, 429);
  }
  const { nickname, content: text, target_type = 'site', target_id = null } = await c.req.json();
  if (!nickname?.trim() || !text?.trim()) return c.json({ detail: '昵称和内容必填' }, 400);
  if (nickname.length > 20) return c.json({ detail: '昵称过长' }, 400);
  if (text.length > 500) return c.json({ detail: '内容过长（500 字以内）' }, 400);
  if (!['diary', 'photo', 'site'].includes(target_type)) return c.json({ detail: '非法目标类型' }, 400);
  await c.env.DB.prepare('INSERT INTO messages (nickname, content, target_type, target_id) VALUES (?, ?, ?, ?)')
    .bind(nickname.trim(), text.trim(), target_type, target_id).run();
  return c.json({ detail: '留言已提交，待审核' }, 202);
});
```

（public.ts 顶部需已 import `rateLimit, clientIp`，Task 4 已引入。）

`worker/src/routes/admin.ts` 追加（middleware 处加 `admin.use('/messages', adminAuth); admin.use('/messages/*', adminAuth);`）：

```ts
admin.get('/messages', async (c) => {
  const pendingOnly = c.req.query('pending') === '1';
  const { results } = await c.env.DB.prepare(
    `SELECT * FROM messages ${pendingOnly ? 'WHERE is_approved = 0' : ''} ORDER BY id DESC LIMIT 200`
  ).all();
  return c.json(results);
});

admin.post('/messages/:id/approve', async (c) => {
  await c.env.DB.prepare('UPDATE messages SET is_approved = 1 WHERE id = ?').bind(c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.delete('/messages/:id', async (c) => {
  await c.env.DB.prepare('DELETE FROM messages WHERE id = ?').bind(c.req.param('id')).run();
  return c.json({ ok: true });
});
```

- [ ] **Step 4: 跑测试确认通过** — `cd worker && npx vitest run` → 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add worker/src/routes/ worker/test/messages.test.ts
git commit -m "feat: 留言提交 + 审核流"
```

---

### Task 9: 账号管理 + 站点设置

**Files:**
- Modify: `worker/src/routes/admin.ts`（users、settings 子路由）
- Test: `worker/test/settings.test.ts`

**Interfaces:**
- Produces:
  - `GET /api/admin/users`（不含 password_hash）、`POST /api/admin/users` `{username, password, display_name}`、`PUT /api/admin/users/:id` `{display_name?, password?}`、`DELETE /api/admin/users/:id`（禁止删除自己、禁止删到最后一个）
  - `GET /api/admin/settings` → `{site_name, anniversary_date, passcode_enabled}`；`PUT /api/admin/settings` `{site_name?, anniversary_date?, passcode?}` — `passcode` 传非空字符串=设置/修改，传 `''`=清除，不传=不动

- [ ] **Step 1: 写失败测试**

```ts
import { SELF } from 'cloudflare:test';
import { describe, it, expect, beforeAll } from 'vitest';
import { applyMigrations, adminToken } from './helpers';

let token: string;
beforeAll(async () => { await applyMigrations(); token = await adminToken(); });
const auth = () => ({ Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' });

describe('账号与设置', () => {
  it('新增账号可登录，不能删除自己', async () => {
    const me = await (await SELF.fetch('http://x/api/admin/users', { headers: auth() })).json() as any[];
    expect(me).toHaveLength(1);
    expect(me[0].password_hash).toBeUndefined();

    const del = await SELF.fetch(`http://x/api/admin/users/${me[0].id}`, { method: 'DELETE', headers: auth() });
    expect(del.status).toBe(400);

    const create = await SELF.fetch('http://x/api/admin/users', {
      method: 'POST', headers: auth(),
      body: JSON.stringify({ username: 'partner', password: 'secret123', display_name: '她' }),
    });
    expect(create.status).toBe(200);

    const login = await SELF.fetch('http://x/api/admin/login', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username: 'partner', password: 'secret123' }),
    });
    expect(login.status).toBe(200);
  });

  it('设置访客口令后 status 变化，清除后恢复', async () => {
    let res = await SELF.fetch('http://x/api/admin/settings', {
      method: 'PUT', headers: auth(), body: JSON.stringify({ passcode: 'hello2026', anniversary_date: '2024-02-14' }),
    });
    expect(res.status).toBe(200);
    let status = await (await SELF.fetch('http://x/api/site/status')).json() as any;
    expect(status.passcode_enabled).toBe(true);
    expect(status.anniversary_date).toBe('2024-02-14');

    res = await SELF.fetch('http://x/api/admin/settings', {
      method: 'PUT', headers: auth(), body: JSON.stringify({ passcode: '' }),
    });
    expect(res.status).toBe(200);
    status = await (await SELF.fetch('http://x/api/site/status')).json() as any;
    expect(status.passcode_enabled).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败** — `cd worker && npx vitest run test/settings.test.ts` → FAIL。

- [ ] **Step 3: 实现**

`worker/src/routes/admin.ts` 追加（`admin.use('/settings'...)` middleware 同样加上）：

```ts
// ---- 账号管理 ----
admin.get('/users', async (c) => {
  const { results } = await c.env.DB.prepare(
    'SELECT id, username, display_name, created_at FROM admin_users ORDER BY id'
  ).all();
  return c.json(results);
});

admin.post('/users', async (c) => {
  const { username, password, display_name = '' } = await c.req.json();
  if (!username || !password) return c.json({ detail: '账号和密码必填' }, 400);
  if (password.length < 8) return c.json({ detail: '密码至少 8 位' }, 400);
  const dup = await c.env.DB.prepare('SELECT id FROM admin_users WHERE username = ?').bind(username).first();
  if (dup) return c.json({ detail: '账号已存在' }, 400);
  const r = await c.env.DB.prepare('INSERT INTO admin_users (username, password_hash, display_name) VALUES (?, ?, ?)')
    .bind(username, bcrypt.hashSync(password, 10), display_name).run();
  return c.json({ id: r.meta.last_row_id });
});

admin.put('/users/:id', async (c) => {
  const { display_name, password } = await c.req.json();
  if (password && password.length < 8) return c.json({ detail: '密码至少 8 位' }, 400);
  await c.env.DB.prepare('UPDATE admin_users SET display_name = COALESCE(?, display_name), password_hash = COALESCE(?, password_hash) WHERE id = ?')
    .bind(display_name ?? null, password ? bcrypt.hashSync(password, 10) : null, c.req.param('id')).run();
  return c.json({ ok: true });
});

admin.delete('/users/:id', async (c) => {
  const me = c.get('admin') as { id: number };
  const targetId = Number(c.req.param('id'));
  if (targetId === me.id) return c.json({ detail: '不能删除当前登录账号' }, 400);
  const count = await c.env.DB.prepare('SELECT COUNT(*) AS n FROM admin_users').first<{ n: number }>();
  if ((count?.n ?? 0) <= 1) return c.json({ detail: '至少保留一个账号' }, 400);
  await c.env.DB.prepare('DELETE FROM admin_users WHERE id = ?').bind(targetId).run();
  return c.json({ ok: true });
});

// ---- 站点设置 ----
admin.get('/settings', async (c) => {
  return c.json({
    site_name: await getSetting(c.env.DB, 'site_name'),
    anniversary_date: await getSetting(c.env.DB, 'anniversary_date'),
    passcode_enabled: Boolean(await getSetting(c.env.DB, 'site_passcode_hash')),
  });
});

admin.put('/settings', async (c) => {
  const { site_name, anniversary_date, passcode } = await c.req.json();
  if (site_name !== undefined) await setSetting(c.env.DB, 'site_name', String(site_name));
  if (anniversary_date !== undefined) await setSetting(c.env.DB, 'anniversary_date', String(anniversary_date));
  if (passcode !== undefined) {
    await setSetting(c.env.DB, 'site_passcode_hash',
      passcode === '' ? '' : bcrypt.hashSync(String(passcode), 10));
  }
  return c.json({ ok: true });
});
```

文件顶部补 `import { getSetting, setSetting } from '../guard';`。

- [ ] **Step 4: 跑测试确认通过** — `cd worker && npx vitest run` → 全部 PASS（后端完成）。

- [ ] **Step 5: Commit**

```bash
git add worker/src/routes/admin.ts worker/test/settings.test.ts
git commit -m "feat: 账号管理 + 站点设置（含访客口令设置/清除）"
```

---

### Task 10: Web 脚手架（Vite + Vue3 + 路由 + api.js + Pages Functions）

**Files:**
- Create: `web/package.json`、`web/vite.config.js`、`web/index.html`
- Create: `web/src/main.js`、`web/src/App.vue`、`web/src/router.js`、`web/src/api.js`
- Create: `web/src/style.css`（全局样式：暖色纪念册风格变量）
- Create: `web/functions/api/[[path]].ts`、`web/functions/uploads/[[path]].ts`
- Create: `web/public/_headers`、`web/public/_routes.json`

**Interfaces:**
- Produces:
  - `api.js` 导出 `api(path, options)`（自动带访客/管理员 token、401 分流跳转）、`apiUpload(path, formData)`、`setGuestToken/getGuestToken/clearGuestToken`、`setAdminToken/getAdminToken/clearAdminToken`
  - router 导出路由表与 `installGuards(router)`（访客口令守卫 + 后台守卫），供后续任务注册页面路由
  - `API_BASE = ''`（同源）；dev 由 vite proxy 转发

- [ ] **Step 1: 脚手架**

`web/package.json`：

```json
{
  "name": "cyyinfo-web",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^14.1.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.0",
    "vite": "^5.4.0"
  }
}
```

`web/vite.config.js`：

```js
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': 'http://localhost:8787',
      '/uploads': 'http://localhost:8787',
    },
  },
});
```

`web/src/api.js`：

```js
const GUEST_KEY = 'cyyinfo_guest_token';
const ADMIN_KEY = 'cyyinfo_admin_token';

export const getGuestToken = () => localStorage.getItem(GUEST_KEY) || '';
export const setGuestToken = (t) => localStorage.setItem(GUEST_KEY, t);
export const clearGuestToken = () => localStorage.removeItem(GUEST_KEY);
export const getAdminToken = () => localStorage.getItem(ADMIN_KEY) || '';
export const setAdminToken = (t) => localStorage.setItem(ADMIN_KEY, t);
export const clearAdminToken = () => localStorage.removeItem(ADMIN_KEY);

async function request(path, { method = 'GET', body, admin = false, form = null } = {}) {
  const headers = {};
  const token = admin ? getAdminToken() : getGuestToken();
  if (token) headers.Authorization = `Bearer ${token}`;
  let payload;
  if (form) {
    payload = form;
  } else if (body !== undefined) {
    headers['Content-Type'] = 'application/json';
    payload = JSON.stringify(body);
  }
  const res = await fetch(path, { method, headers, body: payload });
  if (res.status === 401) {
    if (admin) {
      clearAdminToken();
      if (!location.pathname.startsWith('/admin/login')) location.href = '/admin/login';
    } else if (!location.pathname.startsWith('/gate')) {
      clearGuestToken();
      location.href = '/gate';
    }
    throw new Error('未授权');
  }
  const data = await res.json().catch(() => ({}));
  if (!res.ok) throw new Error(data.detail || `请求失败（${res.status}）`);
  return data;
}

export const api = (path, opts) => request(`/api${path}`, opts);
export const apiUpload = (path, formData, admin = true) =>
  request(`/api${path}`, { method: 'POST', form: formData, admin });
```

`web/src/router.js`（页面组件由后续任务创建，先注册占位 import，本任务只建 Home/AdminLogin 占位页，路由逐个任务补充）：

```js
import { createRouter, createWebHistory } from 'vue-router';
import { api, getGuestToken, getAdminToken } from './api';

const routes = [
  { path: '/gate', name: 'gate', component: () => import('./views/GateView.vue'), meta: { public: true } },
  { path: '/', name: 'home', component: () => import('./views/HomeView.vue') },
  { path: '/admin/login', name: 'admin-login', component: () => import('./views/admin/LoginView.vue'), meta: { public: true } },
];

export const router = createRouter({ history: createWebHistory(), routes });

let passcodeEnabled = null; // 缓存 site/status

router.beforeEach(async (to) => {
  if (to.meta.public) return true;
  // 后台守卫
  if (to.path.startsWith('/admin')) {
    return getAdminToken() ? true : { name: 'admin-login' };
  }
  // 访客口令守卫
  if (passcodeEnabled === null) {
    try {
      const s = await api('/site/status');
      passcodeEnabled = s.passcode_enabled;
    } catch { passcodeEnabled = false; }
  }
  if (passcodeEnabled && !getGuestToken()) return { name: 'gate' };
  return true;
});
```

`web/src/main.js`：

```js
import { createApp } from 'vue';
import App from './App.vue';
import { router } from './router';
import './style.css';

createApp(App).use(router).mount('#app');
```

`web/src/App.vue`（本任务先放 `<router-view>`，桌宠组件 Task 12 挂入）：

```vue
<template>
  <router-view />
</template>
```

`web/index.html`：标准 Vite 模板，`title` 写「我们的小站」，`lang="zh-CN"`。

`web/functions/api/[[path]].ts` 与 `web/functions/uploads/[[path]].ts`（同源代理到 Worker，与 ysoho 一致）：

```ts
const WORKER = 'https://cyyinfo-api.<你的子域>.workers.dev'; // 部署任务时替换为真实域名

export const onRequest: PagesFunction = async ({ request, params }) => {
  const url = new URL(request.url);
  const path = (params.path as string[]).join('/');
  const prefix = url.pathname.startsWith('/api') ? '/api' : '/uploads';
  const target = `${WORKER}${prefix}/${path}${url.search}`;
  return fetch(new Request(target, request));
};
```

（两个文件内容相同，prefix 各自写死也可；直接照抄 `ysoho/web/functions/` 两个文件后改域名即可。）

`web/public/_routes.json`：

```json
{ "version": 1, "include": ["/api/*", "/uploads/*"], "exclude": [] }
```

`web/public/_headers`：

```
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
```

- [ ] **Step 2: 建 GateView / HomeView / admin/LoginView 占位页**

`web/src/views/GateView.vue`：全屏居中一个输入框 + 按钮，提交 `api('/passcode/verify', {method:'POST', body:{passcode}})`，成功 `setGuestToken(token)` 后 `router.replace('/')`，失败显示 `detail`。样式：深色或暖色背景 + 单卡片。

`web/src/views/HomeView.vue`：先只渲染「首页（待完善）」。

`web/src/views/admin/LoginView.vue`：账号/密码表单 → `api('/admin/login', {method:'POST', body})` → `setAdminToken` → `router.replace('/admin')`。

- [ ] **Step 3: 手动验证**

```bash
cd worker && npx wrangler dev &   # 终端 1
cd web && npm install && npm run dev  # 终端 2
```

打开 `http://localhost:5173`：
- `/admin/login` 能用 `admin / testpass123`（wrangler dev 的 .dev.vars，见下一步）登录拿到 token 并跳转
- 后台设置口令后，无痕窗口访问 `/` 被弹到 `/gate`，输入口令进入

wrangler dev 本地 secret：在 `worker/.dev.vars` 写（此文件加入 `worker/.gitignore`，不入库）：

```
JWT_SECRET=dev-secret-change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=testpass123
```

- [ ] **Step 4: Commit**

```bash
printf 'node_modules/\ndist/\n.dev.vars\n.wrangler/\n' > .gitignore
git add .gitignore web/
git commit -m "feat: web 脚手架（Vite+Vue3+路由守卫+api 封装+Pages Functions）"
```

---

### Task 11: 全局布局 + 纪念日倒计时首页

**Files:**
- Modify: `web/src/App.vue`（前台页面套 NavBar + MiniPlayer 占位）
- Create: `web/src/components/NavBar.vue`
- Modify: `web/src/views/HomeView.vue`
- Create: `web/src/components/MessageBoard.vue`（留言区通用组件，本任务先用于首页 site 留言）

**Interfaces:**
- Produces:
  - `<NavBar>`：站点名（来自 `/api/site/status`）+ 导航（首页/相册/日记/音乐）+ 当前路由高亮
  - `<MessageBoard targetType="site" :targetId="null">`：留言列表 + 提交框（昵称+内容，提交后提示"待审核"），日记/照片页复用
  - HomeView 数据：`api('/site/status')`、`api('/diaries?page=1')`（取前 3 条）、`api('/albums')`（随机取一张照片）

- [ ] **Step 1: NavBar + App.vue 布局**

App.vue：当前路由以 `/admin` 开头时只渲染 `<router-view>`；否则包一层 `<NavBar>` + `<main class="page"><router-view/></main>` + （Task 16 的 MiniPlayer，先留注释占位）。

- [ ] **Step 2: HomeView 倒计时**

核心逻辑（`days` 计算）：

```js
const days = computed(() => {
  if (!status.value.anniversary_date) return null;
  const start = new Date(status.value.anniversary_date + 'T00:00:00');
  return Math.floor((Date.now() - start.getTime()) / 86400000) + 1;
});
```

模板：大标题「我们在一起 {{ days }} 天」（无日期则显示站点名欢迎语）；下方两栏：最新 3 篇日记卡片（标题+摘要+日期，点击进 `/diaries/:slug`）、随机照片（`/uploads/<filename>`，点击进 `/albums`）；底部 `<MessageBoard targetType="site">`。

- [ ] **Step 3: MessageBoard.vue**

props `{ targetType: String, targetId: Number }`；`onMounted` 拉 `api(`/messages?target_type=${targetType}${targetId ? `&target_id=${targetId}` : ''}`)`；提交 `api('/messages', {method:'POST', body:{nickname, content, target_type, target_id}})` 后清空并提示「留言已提交，审核后显示」。列表项：昵称 + 内容 + 日期。

- [ ] **Step 4: 手动验证** — 首页倒计时正确（先在后台设置纪念日）、日记摘要卡、留言提交后提示待审核、后台批准后首页可见。

- [ ] **Step 5: Commit** — `git add web/ && git commit -m "feat: 首页（倒计时+最新日记+随机照片+留言区）"`

---

### Task 12: 桌宠移植

**Files:**
- Create: `web/public/pet/renderer.js`（从 `cyy/resources/app/renderer.js` 拷贝后改造）
- Create: `web/public/pet/skins/default/skin.json`、`web/public/pet/skins/default/spritesheet.webp`（从 `cyy/resources/app/skins/default/` 原样拷贝）
- Create: `web/public/pet/pet-adapter.js`
- Create: `web/src/components/DesktopPet.vue`
- Modify: `web/src/App.vue`（前台布局挂 `<DesktopPet>`）

**Interfaces:**
- Consumes: skin.json 的 `cell`、`actions`、`behaviors`、`events` 结构
- Produces:
  - `pet-adapter.js` 导出 `createPet(canvas, bubbleEl, skinUrl)` → `{ play(actionName, once?), say(text?), destroy() }`
  - `<DesktopPet>`：右下角 fixed 定位，canvas 渲染桌宠，气泡 div，点击随机动作+台词，桌面端 pointer 拖拽移动

- [ ] **Step 1: 拷贝皮肤资源**

```bash
mkdir -p web/public/pet/skins/default
cp ../cyy/resources/app/skins/default/skin.json web/public/pet/skins/default/
cp ../cyy/resources/app/skins/default/spritesheet.webp web/public/pet/skins/default/
```

- [ ] **Step 2: 改造 renderer.js → pet-adapter.js**

先读 `cyy/resources/app/renderer.js`（~127 行）。核心保留：spritesheet Image 加载、按 `cell` 切帧、`durs` 逐帧时长、`loop/once` 语义、气泡显示。改造点：

1. 删除所有 `window.petAPI` 调用：`invoke('skin-current')` → createPet 参数传入 skinUrl，内部 `fetch(skinUrl)`；`onPet(...)` 删除（改由返回对象的 `play()` 方法驱动）；`send('drag-begin'/'once-done')` → 内部状态处理（once 动作播完自动回 ambient/idle 行为对应的动作）
2. 皮肤内 spritesheet 路径解析：`skin.json` 所在目录 + `spritesheet.webp`
3. 行为映射：把 `behaviors`（如 `ambient` → 某 action）内置为待机循环；随机台词从 `events` 的对话池取

`pet-adapter.js` 骨架（实现时按 renderer.js 实际代码填充）：

```js
export async function createPet(canvas, bubble, skinUrl) {
  const skin = await (await fetch(skinUrl)).json();
  const base = skinUrl.slice(0, skinUrl.lastIndexOf('/') + 1);
  const img = new Image();
  img.src = base + (skin.spritesheet ?? 'spritesheet.webp');
  await img.decode();
  const ctx = canvas.getContext('2d');
  canvas.width = skin.cell.w; canvas.height = skin.cell.h;

  let timer = null, destroyed = false, lineTimer = null;

  function playRow(action, onDone) {
    clearTimeout(timer);
    let i = 0;
    const row = action.row, durs = action.durs;
    (function frame() {
      if (destroyed) return;
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.drawImage(img, (i % durs.length) * skin.cell.w, row * skin.cell.h,
        skin.cell.w, skin.cell.h, 0, 0, canvas.width, canvas.height);
      i++;
      if (i < durs.length || action.loop) {
        timer = setTimeout(frame, durs[i % durs.length]);
      } else onDone?.();
    })();
  }

  const idleAction = skin.actions[skin.behaviors.ambient] ?? Object.values(skin.actions)[0];
  const idle = () => playRow(idleAction);

  function play(name) {
    const action = skin.actions[name];
    if (!action) return;
    playRow(action, idle); // once 播完回待机
    if (action.lines?.length) say(action.lines[Math.floor(Math.random() * action.lines.length)]);
  }

  function say(text) {
    bubble.textContent = text;
    bubble.style.opacity = '1';
    clearTimeout(lineTimer);
    lineTimer = setTimeout(() => { bubble.style.opacity = '0'; }, 3000);
  }

  idle();
  // 随机台词（events 池），每 30-60s
  let ambientTimer = null;
  (function ambient() {
    if (destroyed) return;
    const pool = Object.values(skin.events ?? {}).flat();
    if (pool.length && Math.random() < 0.5) say(pool[Math.floor(Math.random() * pool.length)]);
    ambientTimer = setTimeout(ambient, 30000 + Math.random() * 30000);
  })();

  return {
    play, say,
    actionNames: Object.keys(skin.actions),
    destroy() { destroyed = true; clearTimeout(timer); clearTimeout(lineTimer); clearTimeout(ambientTimer); },
  };
}
```

注意：以上为骨架，实现时必须先读真实 renderer.js 与 default/skin.json，以实际的字段名（`cell` 可能是 `{w,h}` 或 `[w,h]`、behaviors 的 key 名）为准调整。

- [ ] **Step 3: DesktopPet.vue**

```vue
<template>
  <div ref="wrap" class="desktop-pet" :style="{ left: pos.x + 'px', top: pos.y + 'px' }">
    <div ref="bubble" class="pet-bubble"></div>
    <canvas ref="cv" @pointerdown="onDown" @click="onClick"></canvas>
  </div>
</template>
```

逻辑：`onMounted` → `createPet(cv.value, bubble.value, '/pet/skins/default/skin.json')`；点击 → 从 `actionNames` 随机选一个非 idle 动作 `play()`；拖拽：pointerdown 记录偏移，pointermove 更新 `pos`（`transform` 或 left/top），pointerup 结束；区分点击与拖拽（位移 < 5px 视为点击）。初始位置：右下角（`right: 24px; bottom: 24px` → 换算 left/top）。样式：`position: fixed; z-index: 9999; image-rendering: pixelated; cursor: grab;`，气泡绝对定位在 canvas 上方，`transition: opacity .3s`。`onUnmounted` 调 `destroy()`。移动端（`pointer: coarse` 媒体查询）缩小显示尺寸。

- [ ] **Step 4: 手动验证** — 打开首页：右下角桌宠待机动画循环、定时冒台词、点击有动作+台词反应、可拖拽、切路由不消失不重置。

- [ ] **Step 5: Commit** — `git add web/public/pet web/src/components/DesktopPet.vue web/src/App.vue && git commit -m "feat: 桌宠移植（canvas sprite 播放器 + 适配层 + 悬浮组件）"`

---

### Task 13: 相册页面（相册墙 + 照片网格 + 灯箱）

**Files:**
- Create: `web/src/views/AlbumsView.vue`、`web/src/views/AlbumDetailView.vue`
- Create: `web/src/components/Lightbox.vue`
- Modify: `web/src/router.js`（注册 `/albums`、`/albums/:id`）

**Interfaces:**
- Consumes: `api('/albums')`、`api('/albums/:id')`（Worker Task 5）
- Produces: `<Lightbox :photos="[{filename, caption}]" v-model:index="idx">`（左右切换、Esc 关闭、caption 显示）

- [ ] **Step 1: AlbumsView** — 卡片网格：封面（有 `cover_filename` 显示 `/uploads/<f>`，否则纯色占位 + 相册名）、标题、描述、照片数（详情接口才有数量，列表页可只显示标题）。点击进 `/albums/:id`。

- [ ] **Step 2: AlbumDetailView** — 顶部相册标题/描述；照片瀑布流或等比网格（`object-fit: cover` 方格即可）；点击打开 Lightbox。

- [ ] **Step 3: Lightbox.vue** — 全屏 fixed 遮罩，居中 `<img :src="'/uploads/' + photos[index].filename">`，左右箭头 + 键盘 ←/→/Esc（`onMounted` 挂 `keydown`，卸载移除），底部 caption。

- [ ] **Step 4: 手动验证** — 建一个相册传 3+ 张照片，相册墙显示封面，详情网格正常，灯箱切换/关闭正常。

- [ ] **Step 5: Commit** — `git add web/ && git commit -m "feat: 相册墙 + 照片网格 + 灯箱"`

---

### Task 14: 日记页面（列表 + 公众号式阅读页 + 留言区）

**Files:**
- Create: `web/src/views/DiariesView.vue`、`web/src/views/DiaryDetailView.vue`
- Modify: `web/src/router.js`（注册 `/diaries`、`/diaries/:slugOrId`）
- Modify: `web/src/style.css`（markdown 排版样式 `.md-body`）

**Interfaces:**
- Consumes: `api('/diaries?page=')`、`api('/diaries/:slugOrId')`、`marked`（Task 10 已装）、`<MessageBoard targetType="diary" :targetId="diary.id">`

- [ ] **Step 1: DiariesView** — 文章卡片列表：封面图（有则显示）、标题、excerpt、作者、发布日期；底部分页（上一页/下一页，`total` 计算页数）。

- [ ] **Step 2: DiaryDetailView** — 公众号式排版：居中单栏 `max-width: 680px`；封面头图；标题 + 作者 + 日期；正文 `v-html="marked.parse(diary.content_md)"`；底部 `<MessageBoard targetType="diary" :targetId="diary.id">`。

- [ ] **Step 3: `.md-body` 排版样式** — 标题层级、段落行高 1.8、`img { max-width: 100% }`、`blockquote` 左边框、code 块底色、`h2` 分隔线间距等，参照公众号阅读体验。注意 XSS：内容为管理员自写，首版直接用 v-html（ spec 未要求消毒，管理员可信），但加注释说明该前提。

- [ ] **Step 4: 手动验证** — 后台发一篇带标题/图片/引用 md 的日记，列表摘要正常，阅读页排版舒适，留言提交/审核/显示闭环。

- [ ] **Step 5: Commit** — `git add web/ && git commit -m "feat: 日记列表 + Markdown 长文阅读页 + 文章留言"`

---

### Task 15: 音乐页面 + 全局迷你播放器

**Files:**
- Create: `web/src/player.js`（播放器状态单例）
- Create: `web/src/components/MiniPlayer.vue`
- Create: `web/src/views/MusicView.vue`、`web/src/views/MusicAlbumView.vue`
- Modify: `web/src/App.vue`（前台布局挂 `<MiniPlayer>`）
- Modify: `web/src/router.js`（注册 `/music`、`/music/:id`）

**Interfaces:**
- Produces:
  - `player.js` 导出响应式单例：`state: { queue: Song[], index: number, playing: boolean, currentTime: number, duration: number, volume: number }`，方法 `playQueue(songs, startIndex)`、`toggle()`、`next()`、`prev()`、`seek(sec)`、`setVolume(v)`；内部一个模块级 `new Audio()`（不随组件销毁，路由切换不中断）
  - `<MiniPlayer>`：底部 fixed 条，封面/歌名/播放暂停/上下首/进度条/音量；`state.queue` 为空时不渲染

- [ ] **Step 1: player.js**

```js
import { reactive } from 'vue';

const audio = new Audio();

export const state = reactive({
  queue: [], index: -1, playing: false,
  currentTime: 0, duration: 0, volume: 1,
});

function load(i) {
  const song = state.queue[i];
  if (!song) return;
  state.index = i;
  audio.src = `/uploads/${song.filename}`;
  audio.play().catch(() => {});
}

export function playQueue(songs, startIndex = 0) {
  state.queue = songs;
  load(startIndex);
}
export function toggle() {
  if (!state.queue.length) return;
  if (audio.paused) { audio.play().catch(() => {}); } else audio.pause();
}
export const next = () => load((state.index + 1) % state.queue.length);
export const prev = () => load((state.index - 1 + state.queue.length) % state.queue.length);
export const seek = (sec) => { audio.currentTime = sec; };
export const setVolume = (v) => { audio.volume = state.volume = v; };

audio.addEventListener('play', () => { state.playing = true; });
audio.addEventListener('pause', () => { state.playing = false; });
audio.addEventListener('ended', () => next());
audio.addEventListener('timeupdate', () => {
  state.currentTime = audio.currentTime;
  state.duration = audio.duration || 0;
});
```

- [ ] **Step 2: MiniPlayer.vue** — `v-if="state.queue.length"`；进度条用 `<input type="range" :max="state.duration" :value="state.currentTime" @input="seek($event.target.valueAsNumber)">`；音量同理 0-1 step 0.05；显示当前 `state.queue[state.index]?.title`。

- [ ] **Step 3: MusicView / MusicAlbumView** — 专辑封面墙（无封面用渐变占位 + 专辑名）；专辑页：封面 + 年份 + 曲目列表（track_no + title + 右侧播放按钮），点曲目 `playQueue(album.songs, i)`，当前播放曲目高亮。

- [ ] **Step 4: 手动验证** — 后台传 2 首歌，专辑页点击播放、迷你播放器控制正常、切路由播放不中断、播完自动下一首。

- [ ] **Step 5: Commit** — `git add web/ && git commit -m "feat: 音乐专辑页 + 全局迷你播放器"`

---

### Task 16: 后台布局 + 照片管理

**Files:**
- Create: `web/src/views/admin/AdminLayout.vue`（侧边栏：照片/日记/音乐/留言/账号/设置 + 退出）
- Create: `web/src/views/admin/PhotosView.vue`
- Modify: `web/src/router.js`（`/admin` 嵌套路由，children 挂 AdminLayout）

**Interfaces:**
- Consumes: Worker Task 5 全部管理接口
- Produces: 嵌套路由结构 `{ path: '/admin', component: AdminLayout, children: [...] }`，后续后台页面作为 children 追加

- [ ] **Step 1: AdminLayout** — 左侧 nav + 右侧 `<router-view>`；退出按钮 `clearAdminToken(); router.replace('/admin/login')`。路由：`{ path: '/admin', component: () => import('./views/admin/AdminLayout.vue'), children: [{ path: '', redirect: '/admin/photos' }, { path: 'photos', component: ... }] }`（后台路由同样需要 admin 守卫，router.js 的 `to.path.startsWith('/admin')` 分支已覆盖，`/admin/login` 保持 meta.public）。

- [ ] **Step 2: PhotosView** — 上排相册管理（新建相册表单、相册列表：改名/删除/排序值编辑）；选中某相册后下方照片区：`input[type=file][multiple][accept="image/*"]` 选择后逐张 `apiUpload('/admin/photos', form)`（form 带 file/album_id/caption），缩略图网格，每张可编辑 caption、删除、点「设为封面」调 `POST /api/admin/albums/:id/cover`。

- [ ] **Step 3: 手动验证** — 建相册、批量传图、改 caption、设封面、删照片、删相册（确认 R2 文件也被删：前台该图 404）。

- [ ] **Step 4: Commit** — `git add web/ && git commit -m "feat: 后台布局 + 相册/照片管理"`

---

### Task 17: 后台日记编辑器（Markdown + 预览 + 封面上传）

**Files:**
- Create: `web/src/views/admin/DiariesView.vue`（列表）
- Create: `web/src/views/admin/DiaryEditView.vue`（新建/编辑共用）
- Modify: `web/src/router.js`（children 追加 `diaries`、`diaries/new`、`diaries/:id/edit`）

**Interfaces:**
- Consumes: Task 6 管理接口；`marked`

- [ ] **Step 1: DiariesView** — 表格：标题、slug、状态徽章（草稿/已发布）、更新时间；操作：编辑、删除、发布/撤回（PUT status）；「写日记」按钮进 `/admin/diaries/new`。

- [ ] **Step 2: DiaryEditView** — 左右分栏：左侧表单（标题、slug、封面图上传预览、`<textarea>` md 编辑），右侧实时预览 `v-html="marked.parse(content)"`（复用 `.md-body` 样式）；底部保存草稿 / 发布按钮。编辑模式 `onMounted` 从 `api('/admin/diaries')` 找当前 id……（管理端列表不含 content_md，给 Task 6 的管理端补一个 `GET /api/admin/diaries/:id` 返回全字段——作为本任务的子步骤，在 `worker/src/routes/admin.ts` 加：

```ts
admin.get('/diaries/:id', async (c) => {
  const d = await c.env.DB.prepare('SELECT * FROM diaries WHERE id = ?').bind(c.req.param('id')).first();
  if (!d) return c.json({ detail: '日记不存在' }, 404);
  return c.json(d);
});
```

并在 `worker/test/diaries.test.ts` 补一条断言后跑全量测试。）

- [ ] **Step 3: 手动验证** — 新建 → 写 md → 预览同步 → 存草稿（前台不可见）→ 发布（前台可见）→ 编辑回显 → 封面上传。

- [ ] **Step 4: Commit** — `git add web/ worker/ && git commit -m "feat: 后台日记 Markdown 编辑器 + 管理端日记详情接口"`

---

### Task 18: 后台音乐管理 + 留言审核 + 账号管理 + 站点设置

**Files:**
- Create: `web/src/views/admin/MusicView.vue`
- Create: `web/src/views/admin/MessagesView.vue`
- Create: `web/src/views/admin/UsersView.vue`
- Create: `web/src/views/admin/SettingsView.vue`
- Modify: `web/src/router.js`（children 追加四个路由）

**Interfaces:**
- Consumes: Task 7/8/9 全部管理接口

- [ ] **Step 1: MusicView** — 三张专辑卡（改名、年份、封面图上传）；点进专辑展开歌曲表：上传表单（file accept="audio/mpeg,audio/mp4" + 歌名 + 曲目号）、歌曲行（改歌名/曲目号、删除）。

- [ ] **Step 2: MessagesView** — 两个 tab：待审核（批准/删除按钮）、全部；显示昵称/内容/目标（diary+id / photo+id / site）/时间。

- [ ] **Step 3: UsersView** — 账号表（用户名、昵称、创建时间）；新增账号表单；改昵称/重置密码（PUT）；删除（自己那行禁用删除按钮）。

- [ ] **Step 4: SettingsView** — 站点名、纪念日起始日期（date input）、访客口令区：显示当前状态（已启用/未启用），输入新口令保存=设置/修改，「清除口令」按钮（PUT passcode: ''）。

- [ ] **Step 5: 手动验证** — 四个页面各走一遍主流程；设置口令后无痕窗口验证 gate 流程；清除后恢复公开。

- [ ] **Step 6: Commit** — `git add web/ && git commit -m "feat: 后台音乐/留言/账号/设置页面"`

---

### Task 19: 音乐批量导入脚本

**Files:**
- Create: `scripts/upload_music.py`
- Create: `scripts/README.md`

**Interfaces:**
- 输入：本地目录 `music/<专辑名>/<曲目号>_<歌名>.mp3|m4a`
- 输出：调 `wrangler r2 object put cyyinfo-uploads music/<uuid>.<ext>` 上传，并生成 `scripts/out/songs_seed.sql`（INSERT 语句，匹配 migration 里预置专辑的 title 关联 album_id）

- [ ] **Step 1: 实现脚本**

参考 `ysoho/compress_and_upload.py` 的调用方式。核心逻辑：

```python
#!/usr/bin/env python3
"""批量导入音乐到 R2 并生成 songs 表 SQL。
用法: python scripts/upload_music.py <music_dir>
目录结构: <music_dir>/<专辑名>/<曲目号>_<歌名>.mp3
专辑名必须与后台/music_albums 表中的 title 完全一致（David Tao / I'm OK / 黑色柳丁）。
"""
import re
import subprocess
import sys
import uuid
from pathlib import Path

BUCKET = "cyyinfo-uploads"

def main(music_dir: Path) -> None:
    out_dir = Path(__file__).parent / "out"
    out_dir.mkdir(exist_ok=True)
    sql_lines = []
    for album_dir in sorted(p for p in music_dir.iterdir() if p.is_dir()):
        album = album_dir.name
        for f in sorted(album_dir.iterdir()):
            m = re.match(r"(\d+)[_\- ]+(.+)\.(mp3|m4a)$", f.name, re.IGNORECASE)
            if not m:
                print(f"跳过（文件名不符合 曲目号_歌名.mp3）: {f}")
                continue
            track_no, title, ext = int(m.group(1)), m.group(2), m.group(3).lower()
            key = f"music/{uuid.uuid4()}.{ext}"
            subprocess.run(
                ["npx", "wrangler", "r2", "object", "put", f"{BUCKET}/{key}", "--file", str(f), "--remote"],
                check=True,
            )
            safe = title.replace("'", "''")
            sql_lines.append(
                f"INSERT INTO songs (album_id, title, track_no, filename) "
                f"VALUES ((SELECT id FROM music_albums WHERE title = '{album.replace(chr(39), chr(39)*2)}'), '{safe}', {track_no}, '{key}');"
            )
            print(f"OK {album}/{title} -> {key}")
    sql_path = out_dir / "songs_seed.sql"
    sql_path.write_text("\n".join(sql_lines), encoding="utf-8")
    print(f"\n已生成 {sql_path}\n执行: npx wrangler d1 execute cyyinfo-db --remote --file={sql_path}")

if __name__ == "__main__":
    main(Path(sys.argv[1] if len(sys.argv) > 1 else "music"))
```

`scripts/README.md` 写明用法、文件名规范、wrangler 需已登录（`npx wrangler login`）、脚本在 `worker/` 目录外运行时用 `--config worker/wrangler.toml` 或 cd 进 worker 跑。

- [ ] **Step 2: 手动验证** — 造一个含 2 个测试 mp3 的目录跑一遍（可先去掉 `--remote` 走本地验证解析逻辑），确认 SQL 生成正确。

- [ ] **Step 3: Commit** — `git add scripts/ && git commit -m "feat: 音乐批量导入脚本"`

---

### Task 20: 部署上线 + 文档

**Files:**
- Create: `README.md`（项目根：架构、开发命令、部署步骤）
- Modify: `worker/wrangler.toml`（回填真实 database_id）
- Modify: `web/functions/api/[[path]].ts`、`web/functions/uploads/[[path]].ts`（回填真实 Worker 域名）
- Modify: `worker/src/index.ts`（ALLOWED_ORIGINS 加 Pages 域名）

- [ ] **Step 1: 建 Cloudflare 资源**

```bash
cd worker
npx wrangler login
npx wrangler d1 create cyyinfo-db          # 输出 database_id 回填 wrangler.toml
npx wrangler r2 bucket create cyyinfo-uploads
npx wrangler d1 migrations apply cyyinfo-db --remote
npx wrangler secret put JWT_SECRET         # 输入随机长字符串
npx wrangler secret put ADMIN_USERNAME
npx wrangler secret put ADMIN_PASSWORD
npm run deploy                              # 记下 *.workers.dev 域名
```

- [ ] **Step 2: 回填前端代理域名 + CORS**，本地 `npm run build` 验证构建通过。

- [ ] **Step 3: Pages 部署** — Cloudflare Dashboard → Pages → 连接仓库（或 `npx wrangler pages deploy web/dist --project-name=cyyinfo` 先手动发一版），构建命令 `cd web && npm install && npm run build`，输出 `web/dist`。验证：`/api/health` 经 Pages Function 代理可达。

- [ ] **Step 4: 端到端验收清单**（生产环境逐项过）：
  - [ ] 首管理员登录后台
  - [ ] 建第二个账号并登录
  - [ ] 建相册传照片，前台相册墙/灯箱正常
  - [ ] 写日记（md + 封面）发布，前台阅读页正常
  - [ ] 传歌，前台播放、迷你播放器、切页不中断
  - [ ] 桌宠动画/台词/点击/拖拽
  - [ ] 设置纪念日，首页倒计时正确
  - [ ] 留言 → 审核 → 显示
  - [ ] 设置访客口令 → 无痕窗口 gate 流程 → 清除恢复
  - [ ] 批量脚本导入一张专辑

- [ ] **Step 5: README.md** — 记录架构图、dev 命令（worker/web 两个终端）、部署步骤、secret 清单、音乐导入用法。

- [ ] **Step 6: Commit** — `git add -A && git commit -m "docs: README + 部署配置回填"`

---

## 自审记录

- Spec 覆盖：桌宠(T12)、照片(T5/13/16)、日记(T6/14/17)、音乐(T7/15/18/19)、留言(T8/11/14/18)、多账号(T3/9/18)、访客口令(T4/10/18)、纪念日(T4/11)、Pages Functions(T10/20)、部署(T20) — 全覆盖
- T6 管理端缺日记详情接口的问题在 T17 显式补齐（含测试）
- 邮箱通知、歌词、桌宠状态系统按 spec §10 不做
